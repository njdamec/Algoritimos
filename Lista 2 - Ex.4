#include <stdio.h>

int main(void) {
  int v[8], x, y, soma, i;

  printf("Digite seis valores: \n ");
  for(i=0; i<8; i++)
  scanf("%d", &v[i]);

  printf("Valores: ");
        for(int i = 0; i<8; i++) {
          printf("%d; ", v[i]);
        }
    return 0;
  }
