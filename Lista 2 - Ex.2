#include <stdio.h>

void verificarNulo(int valor){
  if (valor==0){
    printf("o valor é nulo.\n");
  }else{
    printf("O valor não é nulo.\n");
  }
}
int main(void) {
  int numero;
  printf("Digite um número: ");
  scanf("%d", &numero);
  verificarNulo(numero);
  return 0;
}
